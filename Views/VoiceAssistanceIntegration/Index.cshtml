@model IEnumerable<WeatherApplication.Models.VoiceAssistanceIntegration>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IntegrationType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Settings)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IntegrationType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Settings)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VoiceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VoiceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VoiceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
