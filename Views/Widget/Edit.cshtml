@model WeatherApplication.Models.Widget

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Widget</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WidgetId" />
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WidgetType" class="control-label"></label>
                <input asp-for="WidgetType" class="form-control" />
                <span asp-validation-for="WidgetType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Settings" class="control-label"></label>
                <input asp-for="Settings" class="form-control" />
                <span asp-validation-for="Settings" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
